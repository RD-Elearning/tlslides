/**
 * Motion recipe for tls.m.image — fade-up entrance.
 */

import type { MotionRecipe } from '../../../types'

export const motion: MotionRecipe = {
  parts: ['image', 'caption'],
  preset: 'fade-up',
}
