/**
 * Motion recipe for tls.m.icon-label — fade-up entrance.
 */

import type { MotionRecipe } from '../../../types'

export const motion: MotionRecipe = {
  parts: ['icon', 'label'],
  preset: 'fade-up',
}
