/**
 * Motion recipe for tls.c.hero — staggered entrance.
 */

import type { MotionRecipe } from '../../../types'

export const motion: MotionRecipe = {
  parts: ['kicker', 'title', 'subtitle', 'cta'],
  preset: 'fade-up',
}
